# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently a minimal/empty starter repo with no application code, dependencies, or services. It contains only a `README.md` and a `Test` file.

- **No build system or package manager** is configured.
- **No services** need to be started.
- **No tests or linting** are configured.
- **No update script dependencies** are required beyond a no-op.

When application code is added, this section should be updated with relevant development setup instructions, service startup caveats, and testing guidance.
